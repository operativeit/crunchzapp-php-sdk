<?php
return [
    'timeout' => 30, // timeout in seconds
    /*
    |--------------------------------------------------------------------------
    | Channel Configuration
    |--------------------------------------------------------------------------
    */
    'channel' => [
        /*
        |--------------------------------------------------------------------------
        | Channel Token
        |--------------------------------------------------------------------------
        */
        'token' => env('CRUNCHZAPP_CHANNEL_TOKEN'),
    ],
    /*
    |--------------------------------------------------------------------------
    | Global Configuration
    |--------------------------------------------------------------------------
    */
    'global' => [
        /*
        |--------------------------------------------------------------------------
        | Global Token
        |--------------------------------------------------------------------------
        */
        'token' => env('CRUNCHZAPP_GLOBAL_TOKEN')
    ],
    /*
    |--------------------------------------------------------------------------
    | One Time Password Configuration
    |--------------------------------------------------------------------------
    */
    'otp' => [
        'is_global' => env('CRUNCHZAPP_GLOBAL_OTP', false),
        /*
        |--------------------------------------------------------------------------
        | OTP Code Configuration
        |--------------------------------------------------------------------------
        */
        'code' => [
            /*
            |--------------------------------------------------------------------------
            | Length of the code
            |--------------------------------------------------------------------------
            */
            'length' => 4,
            /*
            |--------------------------------------------------------------------------
            | Options to have letter in code
            |--------------------------------------------------------------------------
            */
            'useLetter' => true,
            /*
            |--------------------------------------------------------------------------
            | Options to have number in code
            |--------------------------------------------------------------------------
            */
            'useNumber' => true,
            /*
            |--------------------------------------------------------------------------
            | Options to have all capital for all code letter
            |--------------------------------------------------------------------------
            */
            'allCapital' => true,
            /*
            |--------------------------------------------------------------------------
            | The name of application who send this or act as identifier
            |--------------------------------------------------------------------------
            */
            'name' => env('APP_NAME'), // otp name
            /*
            |--------------------------------------------------------------------------
            | Otp expiration time ( in seconds )
            |--------------------------------------------------------------------------
            */
            'expires' => 1800 // 30 minutes
        ],
        /*
        |--------------------------------------------------------------------------
        | OTP Link Configuration
        |--------------------------------------------------------------------------
        */
        'link' => [
            /*
            |--------------------------------------------------------------------------
            | OTP Link expiration time ( in seconds )
            |--------------------------------------------------------------------------
            */
            'expires' => 1800,
            /*
            |--------------------------------------------------------------------------
            | Prompt from user
            |--------------------------------------------------------------------------
            */
            'prompt' => 'Give me a code to login at '.env('APP_NAME'),
            /*
            |--------------------------------------------------------------------------
            | The name of application who send this or act as identifier
            |--------------------------------------------------------------------------
            */
            'name' => env('APP_NAME'),
            /*
            |--------------------------------------------------------------------------
            | Respond from channel to user
            |--------------------------------------------------------------------------
            */
            'respond' => [
                /*
                |--------------------------------------------------------------------------
                | Response when OTP is validated, correct prompt and correct phone number
                |--------------------------------------------------------------------------
                */
                'success' => 'Here is your code, make sure it still safe and secret!',
                /*
                |--------------------------------------------------------------------------
                | Response when OTP is failed this caused by different phone number or
                | Modified prompts
                |--------------------------------------------------------------------------
                */
                'failed' => 'Your number or code was not valid',
                /*
                |--------------------------------------------------------------------------
                | Response when OTP is expired
                |--------------------------------------------------------------------------
                */
                'expired' => 'This link was expired. Please request a new link'
            ],
            'callback' => [
                /*
                |--------------------------------------------------------------------------
                | Success callback url
                |--------------------------------------------------------------------------
                */
                'success' => 'https://www.domain.com/validate/success',
                /*
                |--------------------------------------------------------------------------
                | Failed callback url
                |--------------------------------------------------------------------------
                */
                'failed' => 'https://www.domain.com/validate/failed'
            ]
        ]
    ]
];
